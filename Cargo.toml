[workspace]
members = [
  "rust",
  "jmdict",
  "dictionary",
  "unidic",
  "unidic-types",
  "extra/generate-license/rust",
  "extra/tokenizers",
]
resolver = "2"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "z"
