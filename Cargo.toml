[workspace]
members = [
    "rust",
    "dictionary",
    "dictionary-types",
    "unidic",
    "unidic/unidic-build",
]
resolver = "2"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "z"
