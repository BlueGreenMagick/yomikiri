[workspace]
members = [
    "rust",
    "dictionary",
    "dictionary-types",
    "unidic",
    "unidic/unidic-build"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "z"