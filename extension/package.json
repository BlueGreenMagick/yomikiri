{
  "name": "@yomikiri/extension",
  "version": "0.0.1",
  "description": "Web extension to view definition for Japanese words and add to Anki.",
  "main": "index.js",
  "author": "BlueGreenMagick",
  "license": "MIT",
  "scripts": {
    "build": "cross-env NODE_ENV=production yarn run build:types && node esbuild.config.mjs",
    "dev": "rimraf build ; cross-env NODE_ENV=development node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.233",
    "@types/pako": "^2.0.0",
    "cross-env": "^7.0.3",
    "esbuild": "^0.17.18",
    "esbuild-plugin-copy": "^2.1.1",
    "esbuild-svelte": "^0.7.3",
    "rimraf": "^5.0.0",
    "svelte": "^3.58.0",
    "svelte-preprocess": "^5.0.3",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@yomikiri/tokenizer": "0.0.1",
    "pako": "^2.1.0"
  }
}
