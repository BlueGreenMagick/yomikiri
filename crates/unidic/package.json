{
  "name": "@yomikiri/unidic-rs",
  "collaborators": [
    "BlueGreenMagick <bluegreenmagick@gmail.com>"
  ],
  "description": "",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "format": "cargo fmt; pnpm -w run _prettier -- \"$PWD\"",
    "check": "cargo check",
    "lint": "cargo clippy -- -D warnings",
    "test": "cargo test",
    "generate": "cargo run -- --clean --unidic-dir='original' --transform-dir='transformed' --output-dir='output' --resource-dir='../yomikiri-dictionary-generator/files'"
  }
}
